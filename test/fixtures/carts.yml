# == Schema Information
#
# Table name: carts
#
#  id         :bigint           not null, primary key
#  subtotal   :decimal(, )
#  total      :decimal(, )
#  created_at :datetime         not null
#  updated_at :datetime         not null
#

one:
  is_empty: true

two:
  is_empty: false
